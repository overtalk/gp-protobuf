syntax = "proto3";

package protocol;

import "proto/common.proto";

option go_package = "go;protocol";

// 题目分类列表（get）
message ProblemList
{
    repeated string categories = 1;     // 所有类别
    repeated Problem problems = 2;      // 默认题目
}