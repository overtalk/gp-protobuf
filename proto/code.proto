syntax = "proto3";

package protocol;

option go_package = "go;protocol";

// Code : 状态码
enum Code {
    OK = 0;
    INTERNAL = 1;       // 服务端内部错误
    INVAILD_DATA = 2;   // 非法数据，post数据无法反序列化
    PERMISSION_DENIED = 3;  // 没有token，无法身份认证
}